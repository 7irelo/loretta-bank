spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/loretta_audit_test
    username: loretta
    password: loretta_secret
  jpa:
    hibernate:
      ddl-auto: create-drop
  flyway:
    enabled: false
  kafka:
    bootstrap-servers: localhost:9092

eureka:
  client:
    enabled: false
