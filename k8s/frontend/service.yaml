apiVersion: v1
kind: Service
metadata:
  name: loretta-frontend
  labels:
    app: loretta-frontend
spec:
  type: ClusterIP
  selector:
    app: loretta-frontend
  ports:
    - name: http
      port: 80
      targetPort: http
